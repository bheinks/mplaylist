A simple MPlayer-compatible playlist generator.

Requirements:
* Python 3.2.3+
* MPlayer

To install this program, copy it to a location in the system $PATH, or store
it elsewhere and create a symbolic link to it in $PATH. The program itself
does not require any further installation.

See "playlist -h" for information concerning arguments and flags.
